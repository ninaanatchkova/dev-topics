# 03.07.2026

## Videos

### [How does !important actually work? (It's not what you think!)](https://www.youtube.com/watch?v=dS123IXPcJ0) (video length: 7:45)

**Reason**: Wanted more background on why styles declared with !important on a CSS Cascade Layer with the least precedence take precedence over styles declared with !important on cascade layers with more precedence

#### Notes

According to the [CSS Cascading and Inheritance Module Level 5](https://drafts.csswg.org/css-cascade-5/#importance) spec:

**CSS attempts to create a balance of power between author and user style sheets. By default, rules in an author’s style sheet override those in a user’s style sheet, which override those in the user-agent’s default style sheet. To balance this, a declaration can be marked important, which increases its weight in the cascade and inverts the order of precedence.**

**CSS: Cascade origin**

- User agent (web browser default styles)
- Local user (user accessiblity and other preferences such as font-size or contrast)
- Author styles (styles authored by the author of the webpage)

**Least to most specific styles**

USER AGENT (base) < LOCAL USER (base) < AUTHOR STYLES (base) < AUTHOR STYLES !important < LOCAL USER !important < USER AGENT !important

- With cascade layers, unlayered styles are more important than layered styles

Author @layer < Author normal < Author normal !important < Author @layer !important

**Conclusion**: Reason for !important behaviour was not explicitly stated, but it looks like it is meant to invert precedence rather than bump precedence (the way increasing specificity would)

## Articles

### [Day 1: custom properties and fallbacks](https://www.matuzo.at/blog/2022/100daysof-day1)

**Reason:** Came across [#100DaysOfMoreOrLessModernCSS. ](https://www.matuzo.at/blog/2022/100-days-of-more-or-less-modern-css/) while researching and also didn't know some of these new CSS features

#### Notes

- You can pass a second value to the var() CSS function which acts as a fallback for when the property has not been set.

`div {
background-color: var(--not-set-variable, #000);
}

/_ Result: #000 background _/`

- The fallback can also be a custom property (with its own fallback).

`div {
background-color: var(--not-set-variable, var(--not-set-backup-variable, #00F));
}
/_ Result: #00F background _/`

- Unlike invalid values, which are ignored, when they override valid values, a css variable with a value that's not set will still override a previous declaration

`div {
  background-color: #F00;
  background-color: lol;
}
/* Result: #F00 background */`

`div {
  background-color: #F00;
  background-color: var(--not-set);
}
/* Result: transparent background */`

**Conclusion** Useful for theming, especially in cases when dynamically set css variables are used

---

### [A Look Inside Vue's Change Detection](https://brophy.org/post/a-look-inside-vues-change-detection)

**Reason**: I've had it open for a while

#### Notes

- Change detection approaches such as dirty checking and vdom diffing (even when you explicitly delare only a subset of properties which should be tracked) couples template and model tightly (which can lead to potential maintenance issues)
